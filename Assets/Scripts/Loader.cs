using UnityEngine;
using System.Collections;

public class Loader : MonoBehaviour
{
    /*
    public GameObject gameManager;

    private void Awake()
    {
        if(GameManager.instance == null)
        {
            Instantiate(gameManager);
        }

        KeyValuePair<string, int> course = new KeyValuePair<string, int>("2D Game Making in Unity", 01);
        course.print();

        KeyValuePair<string, string> author = new KeyValuePair<string, string>("Don't know", "Best course ever");
        author.print();
    }
    
}

public class KeyValuePair<Tkey, TValue>
{
    public Tkey key;
    public TValue value;

    //initializer
    public KeyValuePair(Tkey _key, TValue _value)
    {
        key = _key;
        value = _value;
    }

    public void print ()
    {
        Debug.Log("key: " + key.ToString());
        Debug.Log("Value: " + value.ToString());
    }
    */
}
